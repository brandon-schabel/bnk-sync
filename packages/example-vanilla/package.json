{
  "name": "bnk-websocket-manager-example-vanilla",
  "module": "src/index.ts",
  "main": "src/index.ts",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "test": "bun test src/",
    "test:watch": "bun test --watch src/",
    "prepublishOnly": "bun test",
    "publish:websocket-manager": "bun publish --access public",
    "dev:example": "bun example-bun-server.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "@bnk/websocket-manager": "workspace:*"
  }
}